export default function Home() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-24 text-center">
      <h1 className="text-4xl font-bold tracking-tight">Matchday</h1>
      <p className="mt-4 text-muted-foreground">
        World Cup 2026 Fan Travel Planner — UI lands in Phase 2. The API is live:
        try <code className="text-primary">/api/matches</code>.
      </p>
    </main>
  );
}
